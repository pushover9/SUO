<?xml version="1.0" encoding="UTF-8"?>


<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="User"><!-- 유저관련 -->
 	
 	<insert id="userJoin">
 	 insert into user_account values(user_acc_seq.nextval,#{user_id},#{user_pw},#{user_name},
 	 #{user_email01},#{user_email02},#{user_phone01},#{user_phone02},#{user_phone03},sysdate)
 	</insert>
 	
 	<select id="user_login" resultType="u">
 	 select * from user_account where user_id=#{user_id}
 	</select>
 	
 	<select id="overlab" resultType="u">
 	 select * from user_account where user_id=#{user_id}
 	</select>
 	
 	<select id="find_id" resultType="u">
 	 select * from user_account where user_name=#{user_name} and user_email01=#{user_email01} 
 	 and user_email02=#{user_email02}
 	</select>
 	
 	<select id="find_pw" resultType="u">
 	 select * from user_account where user_name=#{user_name} and user_email01=#{user_email01} 
 	 and user_email02=#{user_email02} and user_id=#{user_id}
 	</select>
 	
 	<update id="update_pw">
 	 update user_account set user_pw=#{user_pw} where user_id=#{user_id}
 	</update>
 	
 	<select id="edit_get" resultType="u">
 	 select * from user_account where user_id=#{user_id}
 	</select>
 	
 	<update id="edit_user">
 	 update user_account set user_pw=#{user_pw}, user_phone01=#{user_phone01}, user_phone02=#{user_phone02}
 	 , user_phone03=#{user_phone03}, user_email01=#{user_email01}, user_email02=#{user_email02}
 	 where user_id=#{user_id}
 	</update>
 	
 	<select id="search_user" resultType="String">
 	 select * from user_account where user_id=#{user_id}
 	</select>
 	
 
 </mapper>